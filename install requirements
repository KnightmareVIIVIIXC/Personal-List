>>>>>^<<<<<
> Node.js <
>> Install node.js with additional tools, then run the following `npm` command in a console window <<

npm i -g @adguard/hostlist-compiler

>>>>><<<<<
> Python <
>> Run the `pip install` command in a console window to install requirements <<

pip install -r requirements.txt

>> Run the `pip freeze` command in PowerShell to update all requirements <<

pip freeze | %{$_.split('==')[0]} | %{pip install --upgrade $_}
